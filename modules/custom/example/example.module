<?php

/**
 * @file
 * Register a module or theme's theme implementations as well as other function override as per requirement.
 */

/**
 * Implements hook_theme()
 */
function example_theme($existing, $type, $theme, $path) {
  return [
    'time_zone_block' => [
      'variables' => [
        'result' => NULL,
        'timestamp' => NULL
      ],
    ],
  ];
}
